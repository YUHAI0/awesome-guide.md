# Contributing

请通过 GitHub Issue 提交新条目，标题使用：`Add: 网站名`。

提交前请确认：

- Markdown 指南 URL 是公开可访问的 `.md` 页面。
- 该页面由网站、产品或文档站主动提供给 agent 使用。
- “agent 可以根据它完成什么动作”不超过 40 字。
- “发现入口或公开来源”不超过 40 字。
- 已提供公开来源，并勾选模板中的验证状态。

Issue 校验通过后，需要仓库管理者（admin 权限）添加 `approved` 标签确认，Action 才会写入 README。
